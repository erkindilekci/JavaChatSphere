<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ChatSphere" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/dark_gray</item>
        <item name="actionOverflowButtonStyle">@style/ThreeDotColor</item>
    </style>

    <style name="ThreeDotColor" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="tint">#FFFFFF</item>
    </style>

    <style name="Theme.ChatSphere" parent="Base.Theme.ChatSphere" />
</resources>